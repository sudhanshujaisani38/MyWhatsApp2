<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"

    android:padding="4dp">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/circleImageMessageBody"
        android:layout_width="0dp"
        android:layout_height="0dp"

        android:src="@drawable/images"

        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/textViewMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:layout_marginBottom="4dp"
        android:background="@drawable/message_box_recieved"
android:maxWidth="275dp"
        android:backgroundTint="#ffffff"
        android:fontFamily="sans-serif"
        android:padding="15dp"
        android:text="TextView"
        android:textColor="@android:color/black"
        android:textSize="15dp"
        app:layout_constraintBottom_toTopOf="@+id/imageViewMesage"
        app:layout_constraintEnd_toStartOf="@+id/circleImageMessageBody"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/imageViewMesage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/message_box_sent"
        android:maxWidth="275dp"
        android:maxHeight="275dp"
        android:padding="4dp"
        android:scaleType="centerInside"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/circleImageMessageBody"

        app:layout_constraintTop_toBottomOf="@+id/textViewMessage" />

</android.support.constraint.ConstraintLayout>